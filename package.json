{
  "name": "liteweb-css",
  "private": true,
  "entry": "dist/style.css",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf ./dist && vite build --config config.vite.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.55.0",
    "vite": "^3.1.0"
  }
}
